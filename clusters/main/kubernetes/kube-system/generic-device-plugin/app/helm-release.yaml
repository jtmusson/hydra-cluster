---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: generic-device-plugin
  namespace: kube-system
spec:
  interval: 15m
  chart:
    spec:
      chart: generic-device-plugin
      version: 2.4.2
      sourceRef:
        kind: HelmRepository
        name: truecharts
        namespace: flux-system
      interval: 15m
  timeout: 20m
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    global:
      stopAll: false
    image:
      tag: 1478221@sha256:f8f9bdd0cde97295a6a5ac7993957e11df6c134d8e1c95ba50b6b91ac7fc6bb1
    podOptions:
      nodeSelector:
        kubernetes.io/hostname: "k8s-control-1"
    resources:
      requests:
        cpu: 10m
    workload:
      main:
        podSpec:      
          containers:
            main:
              args:
                - --domain
                - musson.uk
                - --device
                - |
                  name: optical
                  groups: 
                    - paths: # NOTE: device numbering may change if a device is added or removed from the system
                      - path: /dev/sr0
                      - path: /dev/sg0